name: qtade-tn-fluids
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - scipy
  - matplotlib
  - jupyterlab
  - nbstripout
  # Parts III and IV. The linear solver for A x = b is ours, not quimb's.
  - quimb
  - pip
  # - pip:
  #   - torchtt        # optional: AMEn, for comparison in the exercises
